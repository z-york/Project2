Zach York's Train Project:

I implemented all of the basic features (except the trainview one which is not working). I then made better looking rails (having two parallel rails. I then approximated arclength parameterization for speed.